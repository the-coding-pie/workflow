import React from "react";
import { LabelObj } from "../../types";

interface Props {
  label?: LabelObj;
}

const BoardLabelModal = ({ label }: Props) => {
  return <div>BoardLabelModal</div>;
};

export default BoardLabelModal;
