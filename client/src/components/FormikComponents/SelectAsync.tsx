import React from "react";

interface Props {
  label: string;
  id: string;
  name: string;
  optional?: boolean;
  inline?: boolean;
  classes?: string;
}

const SelectAsync = () => {
  return <div></div>;
};

export default SelectAsync;
